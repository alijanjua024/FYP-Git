<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fyp.cricintell">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/cilogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cilogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".AnalysisLogActivity"></activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".TeamDetailsActivity" />
        <activity android:name=".PredictionResultActivity" />
        <activity android:name=".AnalysisResultActivity" />
        <activity android:name=".PlayerDetailsActivity" />
        <activity android:name=".PlayersListActivity" />
        <activity android:name=".PredictionActivity" />
        <activity android:name=".AnalysisActivity" />
        <activity android:name=".TeamsActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>